interface MarkerClusterProps {
    markers: Quest[];
    setMarkers: React.Dispatch<React.SetStateAction<Quest[]>>;
}

interface MapEventsWrapperProps {
    markers: Quest[];
    setMarkers: React.Dispatch<React.SetStateAction<Quest[]>>;
}